const valid = require('./public');
const home = require('./home');
const search = require('./search');
const notFound = require('./notfound');

module.exports = {
  valid,
  home,
  search,
  notFound,
};
